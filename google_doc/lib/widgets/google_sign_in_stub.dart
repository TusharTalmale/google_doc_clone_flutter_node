import 'package:flutter/material.dart';

// Stub for the web-only renderButton method to allow compilation on mobile
Widget renderButton({dynamic configuration}) {
  return const SizedBox.shrink();
}
